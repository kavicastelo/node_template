const {configureServer, advancedConfigureServer, testGet} = require('./serverConfig');
const createModel = require('./serverModel');

module.exports = {
    configureServer,
    advancedConfigureServer,
    testGet,
    createModel
}